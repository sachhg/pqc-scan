# Fixture: nginx TLS configuration with quantum-vulnerable settings.
server {
    listen 443 ssl;
    server_name example.com;

    # PQC012 — allows outdated TLS 1.0 / 1.1.
    ssl_protocols TLSv1 TLSv1.1 TLSv1.2;

    # PQC012 — RSA / ECDSA / 3DES cipher suites.
    ssl_ciphers 'ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES128-SHA256:DES-CBC3-SHA';

    ssl_certificate     /etc/ssl/certs/example.crt;
    ssl_certificate_key /etc/ssl/private/example.key;
}
